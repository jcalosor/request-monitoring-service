NODE_ENV=development
APP_PORT=4000
APP_HOST=0.0.0.0

DB_DRIVER=mongodb

MONGODB_PORT=27017
MONGODB_DB=request-monitor

PING_INTERVAL=1

# Push notification credentials
PUSH_CHANNEL="request_monitor"
PUSH_EVENT="new_request"
PUSH_APP_ID=
PUSH_APP_KEY=
PUSH_APP_SECRET=
PUSH_APP_CLUSTER=
PUSH_APP_TLS=